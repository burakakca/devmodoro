/**
 * Analytics-specific formatters.
 * Re-exports shared formatters and provides analytics-specific aliases.
 */

// Re-export shared formatters
export {
	formatDurationLong as formatDuration,
	formatHour,
	formatRelativeDate,
	formatTime,
} from "@/lib/formatters";
